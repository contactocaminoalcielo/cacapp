# Copiar a .env en el servidor. PGPASSWORD y JWT_SECRET se toman del
# .env de Supabase (/opt/supabase/docker/.env). JOB_TOKEN: openssl rand -hex 24
PORT=8787
PGHOST=supabase-db
PGPORT=5432
PGUSER=postgres
PGDATABASE=postgres
PGPASSWORD=
JWT_SECRET=
JOB_TOKEN=
